package com.gdx.kaps.level.grid.germ;

import com.gdx.kaps.level.grid.Color;

public class BasicGerm extends Germ {
    BasicGerm(int x, int y, Color color) {
        super(x, y, color, GermRecord.BASIC);
    }
}
